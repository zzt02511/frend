<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Agent Development Status
##  Subagent的使用纪律
1 未经批准，禁止开 Subagent。
2  除非特殊任务，不要打开 GPT-5.6-Sol 的 Ultra 档位。

## Product Goal

Build a WeChat private-domain live streaming MVP for product sales:

- Mobile host can walk around and introduce products.
- Audience enters from WeChat H5 share links or QR codes.
- Control room can create live rooms, start/end sessions, moderate comments, manage mic requests, and view live status.
- Operations can attribute viewers/customers to the person who shared the room.
- MVP must be maintainable and ready to evolve toward real database, auth, WeChat integration, and LiveKit production usage.

## Current Tech Stack

- Next.js App Router, React, TypeScript.
- Tailwind CSS with shadcn/Radix-style UI primitives.
- Local JSON persistence in `.data/app-store.json` for fast MVP iteration.
- Prisma schema prepared for later database migration.
- Vitest for domain/service tests.

## Completed Progress

- Project scaffold and core live room domain model.
- Admin control room at `/admin`.
- Mobile host console at `/host`.
- Audience H5 room at `/live/[id]`.
- Live session creation and status display: draft, scheduled, live, ended, closed.
- Comment moderation: pending, approve, reject, delete, pin, mark high-value question.
- Mic request flow and participant management.
- WeChat share URL and QR code generation.
- Share attribution with `source` and `sharedBy`.
- Share ranking API and admin ranking table.
- Local persistence for live sessions, comments, stats, share visits, and moderation state.
- Comment analytics API and admin "发言统计" tab.
- Customer lead aggregation API and admin "客户线索" tab.
- Customer follow-up board: room entry, watch duration, questions asked, interest temperature, WeCom status, lead stage, follow-up owner, follow-up status, and follow-up note.
- Customer follow-up state is persisted in local JSON and reflected in the Prisma schema.
- Data repository boundary added: current runtime uses `JsonStoreRepository`, while service/API code can later move to a Prisma-backed repository without changing product flows.
- Docker deployment plumbing added: `Dockerfile`, `docker-compose.yml`, `.dockerignore`, production entrypoint, standalone Next build, and initial Prisma migration for PostgreSQL.
- Deployed to server `218.78.135.21` under `/opt/wechat-live/current` with Docker Compose. App is bound to host port `3001` because host port `3000` is already used by an existing service. Server-local health check `http://127.0.0.1:3001/admin` returns 200; public access requires opening port `3001` in the cloud security group.
- Mobile host console controls now show clear device, start, end, success, and error feedback. The host page also explains that mobile camera/microphone access is blocked on plain HTTP and requires HTTPS for real phone broadcasting.
- HTTPS production entry moved to `live.fuguilong.cn` to avoid overriding the existing `fuguilong.cn` website. The project does not start its own Caddy container in production; the server's existing Caddy is configured to proxy `live.fuguilong.cn` to this app and `/rtc` to self-hosted LiveKit.
- Self-hosted LiveKit Docker service added to Compose. The existing Caddy proxies `/rtc` to LiveKit on the same `live.fuguilong.cn` HTTPS origin, the app signs real LiveKit JWTs, mobile host publishes camera/mic tracks, and audience rooms subscribe to the host stream while keeping existing comment moderation and interaction flows.
- Docker image build now uses locally generated Next.js standalone output instead of building Next.js on the server. This keeps deployment lighter for the 7.5 GB RAM cloud host.
- LiveKit UDP media range is limited to `50000-50100/udp` for MVP deployment to avoid Docker spending a long time creating thousands of port mappings on the cloud host.
- `live.fuguilong.cn` DNS A record is configured and Caddy has obtained a valid Let's Encrypt certificate for the HTTPS production entry. Server-side checks confirm `/host` and `/live/demo-live` return HTTPS 200 through Caddy.
- Product decision: mic request approval belongs on the mobile host console, not the control-room admin. The admin can inspect request status, while the host accepts/rejects during the live session.
- Audience comments in review mode are visible to the sender immediately but hidden from other viewers until approved; the audience UI must not expose the word "审核".
- Audience comment cards must not show backend moderation states such as "已发送", "已展示", or "未展示"; those states are operational metadata and should stay out of the viewer-facing room.
- Approved mic viewers receive LiveKit publish permission and their video appears as a picture-in-picture tile on the host/audience live screen.
- Latest local change before session handoff: `src/components/audience-room.tsx` removed the moderation status badge beside audience comments, and `src/components/audience-room.test.tsx` covers that "已展示/已发送/未展示" are not visible in the audience room.
- Latest verification before session handoff: `npm run test` passed with 11 files / 34 tests, `npm run lint` passed, and `npm run build` passed.
- Latest deployment package before session handoff: `deploy-package.tar.gz` was rebuilt locally and confirmed to include `Dockerfile`, `docker-compose.yml`, `AGENTS.md`, `src/components/audience-room.tsx`, `src/components/audience-room.test.tsx`, and `.next/standalone/node_modules/next/package.json`. It has not yet been uploaded or deployed in this handoff state.
- Production redeploy completed on 2026-06-16: `deploy-package.tar.gz` was uploaded to `/opt/wechat-live/releases/deploy-package.tar.gz`, `/opt/wechat-live/current` was redeployed with Docker Compose, and `.env` kept `RUNTIME_BASE=current-app:latest`.
- Audience LiveKit playback now passes the exact host identity (`roomName-hostUserId`) into `LiveKitAudiencePlayer`, so the host camera track is rendered as the main video instead of being mistaken for a mic guest.
- Mobile host console now polls mic requests every 3 seconds, so new audience mic applications appear without the host leaving and re-entering the live room.
- Mobile host console no longer repeats the live title/status in two stacked top areas on phone screens; the visible title/status lives in the video overlay.
- Latest production verification: `https://live.fuguilong.cn/live/demo-live` and `https://live.fuguilong.cn/host` both returned HTTP 200 through Caddy; deployed bundles were checked for absence of audience comment status labels `已展示`, `已发送`, and `未展示`.
- Latest local verification after the production fixes: `npm run test` passed with 11 files / 37 tests, `npm run lint` passed, and `npm run build` passed.
- Production redeploy completed again on 2026-06-16 for the follow-up live-room fixes: audience online count now polls `/api/live-sessions/[id]/stats` every 3 seconds; `LiveKitAudiencePlayer` has iPhone inline playback attributes, click-to-recover playback messaging, and remote track/participant cleanup; mobile host console has first-screen mic requests, compact 36px controls, first-screen comment visibility, exact host identity filtering, and mic guest preview cleanup on track unsubscribe/participant disconnect.
- Latest production verification on 2026-06-16: `deploy-package.tar.gz` was rebuilt, uploaded to `/opt/wechat-live/releases/deploy-package.tar.gz`, extracted after cleaning remote `.next/standalone` and `.next/static`, and redeployed with Docker Compose while keeping `RUNTIME_BASE=current-app:latest`. `http://127.0.0.1:3001/host`, `https://live.fuguilong.cn/host`, and `https://live.fuguilong.cn/live/demo-live` returned 200; app logs showed Next ready; host HTML includes first-screen mic/comment markers and compact button classes; audience live HTML contains no `已展示`, `已发送`, or `未展示`; stats API returned `ok: true`.
- Latest local fixes pending deployment on 2026-06-16: mobile host console now removes the lower "高价值问题" section, keeps the video area clear and larger, moves mic requests below the video, keeps the four device/live buttons compact below the video, shows the audience interaction area as three compact rows with user ID plus message content and no row borders, and polls host-visible comments every 3 seconds. Mic requests now require a fresh host approval after an earlier approved/connected request; a new application ends stale approved/connected access and revokes audience publish permission until the host taps "通过". Admin comment review now polls live data every 3 seconds and sorts pending comments to the top. Audience and host video elements now set stricter mobile inline playback properties (`defaultMuted` via DOM, `preload=auto`, `playsinline`, `webkit-playsinline`, and a short retry after blocked playback) to reduce iPhone/WeChat no-picture cases.
- Latest local verification for the pending deployment: targeted red/green tests passed, full `npm run test` passed with 13 files / 46 tests, `npm run lint` passed, `npm run build` passed, and local mobile `/host` check on `http://127.0.0.1:4018/host` returned 200 with a 622px video area, mic request panel below the video, four 36px buttons, no "高价值问题", and the interaction panel in the first screen.

- CODE-AUDIT-REPORT.md remediation completed locally on 2026-06-16, pending package/deploy confirmation: live-session PATCH now whitelists editable fields instead of allowing `id`/`roomName`/`status`/`hostUserId` overwrite; whitespace-only comments are rejected before persistence/stats changes; JSON API helpers add baseline security headers; production LiveKit token creation rejects default or too-short secrets; mic approval now requires an `applied` request and refuses muted/banned participants; successful mic count is incremented only on the first valid approval; audience rooms keep the sender's own pending comment during polling; audience rooms send heartbeat POSTs to update watch duration/average watch duration; first share visits no longer collapse to `audience-1` when no viewer id is supplied; replay URLs are generated per live id; `normalizeStore` caches/clones the demo fallback instead of recreating it for every normalization; unused `recordPermissionUpdate` and the unused `questions` host prop were removed.
- Latest audit-remediation verification on 2026-06-16: targeted audit tests passed, full `npm run test` passed with 16 files / 57 tests, `npm run lint` passed, `npm run build` passed, `http://127.0.0.1:4018/host` returned 200 with host video/mic/comment markers present, `http://127.0.0.1:4018/api/live-sessions/demo-live/heartbeat` returned 200, and local `/live/demo-live` rendered without audience-facing `已展示` / `已发送` / `未展示` status labels.
- Production redeploy completed on 2026-06-16 after CODE-AUDIT-REPORT.md remediation: `deploy-package.tar.gz` was rebuilt and checked to contain `Dockerfile`, `docker-compose.yml`, `AGENTS.md`, `src/components/audience-room.tsx`, `src/app/api/live-sessions/[id]/heartbeat/route.ts`, and `.next/standalone/node_modules/next/package.json`; it was uploaded to `/opt/wechat-live/releases/deploy-package.tar.gz`, extracted into `/opt/wechat-live/current` after cleaning remote `.next/standalone` and `.next/static`, and redeployed with Docker Compose while keeping `RUNTIME_BASE=current-app:latest`.
- Latest production verification on 2026-06-16 after audit remediation: server-local `http://127.0.0.1:3001/host`, `/admin`, and `/live/demo-live` returned 200; app logs showed Next ready; public `https://live.fuguilong.cn/host`, `/admin`, and `/live/demo-live` returned 200; public host HTML contained `first-screen-mic-requests` and `host-first-screen-comments`; public stats API returned `ok: true`; and public audience live HTML contained no `已展示` / `已发送` / `未展示` status labels.
- Audit items intentionally left as next-stage architecture/security work rather than half-fixed MVP patches: real authentication/role-based authorization, full request-scoped repository isolation/Prisma storage, and richer route-level loading/error boundaries. Keep these in the Next Development Queue and do not treat the current local JSON MVP as production-auth complete.
- Latest local fixes pending deployment on 2026-06-16: host and audience interaction lists now render oldest-to-newest with the newest message at the bottom, auto-scroll to the bottom on new messages, remain manually scrollable, remove row/card borders, and display the viewer's WeChat-style nickname from `users.name` via enriched comment payloads. Host device/live status text is consolidated into one compact horizontal line below the four 36px controls. Host and audience online displays use refreshed `currentOnline` stats, and participant heartbeats now update `lastActiveAt` so stale viewers drop out of real-time online counts while peak online remains historical.
- Latest local verification for these pending interaction/online-count fixes: targeted tests passed with 4 files / 33 tests, full `npm run test` passed with 16 files / 61 tests, `npm run lint` passed, `npm run build` passed, local `http://127.0.0.1:4018/host`, `/live/demo-live`, and `/api/live-sessions/demo-live/stats` returned 200, and browser smoke checks at a 390px mobile viewport confirmed the host 36px controls, one-line status, no-border interaction area, audience no-border scrollable comments, WeChat nickname rendering, and `currentOnline` display.
- Production redeploy completed on 2026-06-16 for the interaction/online-count fixes: `deploy-package.tar.gz` was rebuilt locally and confirmed to include `Dockerfile`, `docker-compose.yml`, `AGENTS.md`, `src/components/mobile-host-console.tsx`, `src/components/audience-room.tsx`, `.next/static`, and `.next/standalone/node_modules/next/package.json`; it was uploaded to `/opt/wechat-live/releases/deploy-package.tar.gz`; remote `.next/standalone` and `.next/static` were cleaned before extraction into `/opt/wechat-live/current`; `.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`.
- Latest production verification on 2026-06-16 for the interaction/online-count fixes: app logs showed `Next.js 16.2.9` ready; server-local `http://127.0.0.1:3001/host`, `/live/demo-live`, and `/api/live-sessions/demo-live/stats` returned 200; public `https://live.fuguilong.cn/host`, `https://live.fuguilong.cn/live/demo-live`, and public stats API returned 200; public host HTML contained `host-status-line` and `host-first-screen-comments`; public audience HTML contained `audience-comments-panel` and no `已展示` / `已发送` / `未展示` status labels.
- Production disk maintenance completed on 2026-06-17: server disk check showed `/` at 100G total, 49G used, 52G available, 49% usage before cleanup, with inode usage at 4%. Safe Docker cleanup removed stopped containers, dangling images, unused networks, and all unused BuildKit cache; Docker volumes were intentionally not pruned to avoid deleting PostgreSQL/app data. After cleanup `/` was 36G used, 65G available, 36% usage; `docker system df` showed Build Cache `0B`, running Compose services stayed up, server-local `/host`, `/live/demo-live`, and stats returned 200, and public `https://live.fuguilong.cn/host`, `/live/demo-live`, and stats returned 200.
- Latest local fixes pending deployment on 2026-06-17: audience room bottom action changed from like count to "留言互动" and focuses the comment input; audience comment input is borderless with the "发送" button on the same row; the subtitle/description under the audience live title was removed; audience mic button now shows `申请连麦` / `申请中` / `连麦中` state, and a connected audience can end their own mic session through `/mic-requests/[requestId]/end`; host console now shows connected mic guests with a per-guest "结束连麦" action; host and audience LiveKit players now support multiple remote mic video tiles instead of a single guest preview.
- Latest local verification for these pending audience/mic fixes: targeted tests passed with 3 files / 21 tests, full `npm run test` passed with 16 files / 65 tests, `npm run lint` passed, `npm run build` passed, local `http://127.0.0.1:4018/host` and `/live/demo-live` returned 200, host HTML contained `host-connected-mic-guests`, audience HTML contained `audience-comment-compose`, and visible server-rendered audience text no longer contained the live description under the title. These changes were later packaged and deployed in production; see the following production redeploy entry.
- Production redeploy completed on 2026-06-17 for the audience interaction and multi-mic fixes: `deploy-package.tar.gz` was rebuilt locally and confirmed to include `Dockerfile`, `docker-compose.yml`, `AGENTS.md`, `src/components/audience-room.tsx`, `src/components/mobile-host-console.tsx`, `src/components/livekit-audience-player.tsx`, `.next/static`, and `.next/standalone/node_modules/next/package.json`; it was uploaded to `/opt/wechat-live/releases/deploy-package.tar.gz`; remote `.next/standalone` and `.next/static` were cleaned before extraction into `/opt/wechat-live/current`; `.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`.
- Latest production verification on 2026-06-17 for the audience interaction and multi-mic fixes: app logs showed `Next.js 16.2.9` ready; server-local `http://127.0.0.1:3001/host`, `/live/demo-live`, and `/api/live-sessions/demo-live/stats` returned 200; server-local host HTML contained `host-connected-mic-guests`, audience HTML contained `audience-comment-compose`, and the live description was not present in visible server-rendered text; public `https://live.fuguilong.cn/host`, `https://live.fuguilong.cn/live/demo-live`, and public stats API returned 200; public host HTML contained `host-connected-mic-guests`, public audience HTML contained `audience-comment-compose`, visible audience text did not contain the live description, and no `已展示` / `已发送` / `未展示` status labels were present.
- Latest local fixes pending deployment on 2026-06-17: audience status and current-online badges moved to the top-right of the video; audience live title moved to the top-left; the small room/subtitle line above the title is removed; the comment stream now overlays the live video with a transparent background, oldest-to-newest order, newest at the bottom, auto-scroll, and WeChat nickname display; the lower action row now includes `留言互动`, `申请连麦`, and a heart icon for likes; clicking the heart records an approved `某某点赞了主播` interaction row and increments like count; first-time audience entry asks for a WeChat nickname authorization, stores the nickname locally, updates the audience user name through `/join`, and records an approved `某某进入直播间了` interaction row. Normal typed comments still follow the room's review mode.
- Latest local verification for these pending audience overlay/like/nickname fixes: targeted red/green tests passed with 2 files / 17 tests, full `npm run test` passed with 16 files / 68 tests, `npm run lint` passed, and `npm run build` passed. A local dev server smoke on `http://127.0.0.1:4022/live/demo-live` returned 200 and confirmed `audience-title-overlay`, `audience-status-overlay`, `audience-comments-overlay`, and `wechat-auth-panel` are present while the old `LiveKit Room` line is absent. These changes were later packaged and deployed in production; see the following production redeploy entry.
- Latest local fixes pending deployment on 2026-06-17: PC admin tabs now display WeChat nicknames instead of raw WeChat viewer IDs in the lower `待审核留言`, `发言查询`, `发言用户排行`, `客户线索`, `连麦申请`, and `在线用户` areas. Initial `/admin` data and polling APIs now enrich comments, comment analytics, customer leads, mic requests, and participants from `users.name`; UI still falls back to ID only if no nickname exists. The admin `在线用户` tab now uses the same real-time online rule as `stats.currentOnline`: only live sessions count, only recent active non-banned/non-left participants are returned, and ended/scheduled rooms report `currentOnline = 0` with an empty online-user list.
- Latest local verification for the pending PC admin nickname/online fixes: targeted tests passed for `src/components/admin-console.test.tsx` and `src/lib/live-service.test.ts`; full `npm run test` passed with 16 files / 70 tests; `npm run lint` passed; `npm run build` passed; local smoke on `http://127.0.0.1:4023/admin`, `/api/live-sessions/demo-live/stats`, `/participants`, `/mic-requests`, `/leads`, and `/comment-analytics` all returned 200. These changes were later packaged and deployed in production; see the following production redeploy entry.
- Latest local fixes pending deployment on 2026-06-17: PC admin live-room list now supports inline editing of a room title and description through `PATCH /api/live-sessions/[id]`, and supports deleting non-live rooms through `DELETE /api/live-sessions/[id]`. Deleting a room also removes its comments, mic requests, participants, stats, replays, share visits, and customer follow-up rows. Live rooms cannot be deleted: the backend rejects deletion with `LIVE_SESSION_ACTIVE`, and the admin delete button is disabled while a room status is `live`.
- Latest local verification for the pending PC admin live-list edit/delete fixes: targeted tests passed for `src/app/api/live-sessions/[id]/route.test.ts` and `src/components/admin-console.test.tsx`; full `npm run test` passed with 16 files / 74 tests; `npm run lint` passed; `npm run build` passed; local smoke on `http://127.0.0.1:4024/admin`, `/api/live-sessions/demo-live`, `/stats`, and `/participants` returned 200. These changes were later packaged and deployed in production; see the following production redeploy entry.
- Production redeploy completed on 2026-06-17 for the audience overlay/like/nickname authorization, PC admin nickname/real-time online-user, and live-room edit/delete fixes: fresh `npm run test` passed with 16 files / 74 tests, `npm run lint` passed, and `npm run build` passed; `deploy-package.tar.gz` was rebuilt and confirmed to include `Dockerfile`, `docker-compose.yml`, `AGENTS.md`, `src/components/audience-room.tsx`, `src/components/admin-console.tsx`, `src/app/api/live-sessions/[id]/audience-events/route.ts`, `src/app/api/live-sessions/[id]/route.ts`, `.next/static`, and `.next/standalone/node_modules/next/package.json`; it was uploaded to `/opt/wechat-live/releases/deploy-package.tar.gz`; remote `.next/standalone` and `.next/static` were cleaned before extraction into `/opt/wechat-live/current`; `.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`.
- Latest production verification on 2026-06-17 for these fixes: app logs showed `Next.js 16.2.9` ready; server-local `http://127.0.0.1:3001/admin`, `/host`, `/live/demo-live`, `/api/live-sessions/demo-live/stats`, `/participants`, `/mic-requests`, `/comment-analytics`, and `/leads` all returned 200; server-local stats returned `currentOnline: 0` and participants returned an empty list while the demo room was not actively occupied; public `https://live.fuguilong.cn/admin`, `/host`, `/live/demo-live`, `/api/live-sessions/demo-live/stats`, `/participants`, `/mic-requests`, `/comment-analytics`, and `/leads` all returned 200; public audience HTML contained `audience-comments-overlay`, `audience-title-overlay`, `audience-status-overlay`, and `wechat-auth-panel`, did not contain the old `LiveKit Room` subtitle, and did not contain the audience-facing comment status labels `已展示`, `已发送`, or `未展示`; public admin HTML contained edit/delete controls and the online-user tab.
- Post-deploy correction on 2026-06-17: a final verification found `https://live.fuguilong.cn/live/demo-live` returning 500 because the production JSON data volume no longer contained a `liveSessions` record for `demo-live` while still retaining `demo-live` stats/audit references. The data file was backed up inside the app container as `/app/.data/app-store.json.bak-20260617-demo-live-restore`, the `demo-live` room record was restored without deleting existing users/comments/mic data, `app` was restarted, and server-local plus public `/live/demo-live`, live detail, stats, and participants endpoints returned 200 again.
- Production redeploy completed on 2026-06-18 for automatic audience WeChat nickname authorization: the audience entry modal no longer contains a manual nickname input. Clicking `同意授权并进入` calls `/api/live-sessions/[id]/wechat-profile`; if official-account OAuth is configured it can return/redirect through WeChat `snsapi_userinfo`, and if it is not configured the MVP returns an automatic fallback nickname such as `微信观众 r-auto` so viewers can still enter without typing.
- Latest production verification on 2026-06-18 for automatic nickname authorization: targeted tests for `src/components/audience-room.test.tsx` and `src/app/api/live-sessions/[id]/wechat-profile/route.test.ts` passed with 2 files / 11 tests; full `npm run test` passed with 17 files / 76 tests; `npm run lint` passed with no warnings; `npm run build` passed and included `/api/live-sessions/[id]/wechat-profile`; local standalone smoke on `http://127.0.0.1:4025/live/demo-live` and `/api/live-sessions/demo-live/wechat-profile?viewerId=viewer-auto` returned 200 with `wechat-auth-panel` present and `输入微信昵称` absent; `deploy-package.tar.gz` was rebuilt and confirmed to include `src/components/audience-room.tsx`, `src/app/api/live-sessions/[id]/wechat-profile/route.ts`, and `.next/standalone/node_modules/next/package.json`; production `.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`; server-local and public `https://live.fuguilong.cn/live/demo-live`, `/admin`, `/api/live-sessions/demo-live/wechat-profile?viewerId=viewer-auto`, and stats returned 200; public audience HTML contained the auth panel and automatic nickname text, did not contain `输入微信昵称`, and did not contain `已展示`, `已发送`, or `未展示`.
- Production hotfix deployed on 2026-06-18 for host `USER_BANNED` on device startup and audience "agree to enter" appearing unresponsive: production JSON had an old ended test room (`live-3jsifnk`) before `demo-live`, with `host-1` banned in that old room, and `demo-live` was missing again from `liveSessions`. `/host` now uses `selectHostConsoleLiveSession` to prefer `live`, `scheduled`, or `draft` rooms instead of blindly using `store.liveSessions[0]`. Audience automatic authorization now surfaces `/join` failures such as `进入失败：USER_BANNED` instead of silently returning. The production JSON was backed up to `/app/.data/app-store.json.bak-20260618-demo-live-restore`, `demo-live` was restored, and `app` was restarted before deploying the code hotfix.
- Latest production verification on 2026-06-18 for this hotfix: targeted tests for `src/components/audience-room.test.tsx` and `src/lib/live-service.test.ts` passed with 2 files / 23 tests; full `npm run test` passed with 17 files / 78 tests; `npm run lint` passed; `npm run build` passed; package contents included `src/app/host/page.tsx`, `src/lib/live-service.ts`, `src/components/audience-room.tsx`, and `.next/standalone/node_modules/next/package.json`; production `.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`; server-local `/host`, `/live/demo-live`, and `/api/live-sessions/demo-live/wechat-profile?viewerId=viewer-debug` returned 200; public `/live/demo-live`, `/host`, `/api/live-sessions/demo-live/wechat-profile?viewerId=viewer-debug`, and host token POST for `host-1` returned 200; the host token contained `private-demo-live-host-1` and did not contain `USER_BANNED`; public audience HTML still had `wechat-auth-panel`, did not contain `输入微信昵称`, and did not contain `已展示`, `已发送`, or `未展示`.

- Production redeploy completed on 2026-06-18 for the first Tencent Cloud Live/CDN audience playback integration. `LiveSession` now supports `cdnPlayUrl`; the demo room defaults to `webrtc://play.fuguilong.cn/live/IHQDAT`; persisted old JSON rooms are normalized in-place so `demo-live` picks up the CDN play URL without replacing store references; `AudienceRoom` passes the CDN URL into the audience player; and `LiveKitAudiencePlayer` now uses `TencentCloudLivePlayer` for the main audience video while keeping LiveKit connected for mic publishing and remote mic tiles.
- Latest production verification on 2026-06-18 for Tencent Cloud playback: targeted red/green tests for `src/components/audience-room.test.tsx` and `src/components/livekit-audience-player.test.tsx` passed with 2 files / 14 tests; full `npm run test` passed with 17 files / 79 tests; `npm run lint` passed; `npm run build` passed; local standalone smoke on `http://127.0.0.1:4028/live/demo-live` returned 200 and contained `tencent-cloud-live-player` plus `webrtc://play.fuguilong.cn/live/IHQDAT` with no `已展示` / `已发送` / `未展示`; `deploy-package.tar.gz` included `.next/standalone/node_modules/next/package.json`, `src/components/tencent-cloud-live-player.tsx`, `src/components/livekit-audience-player.tsx`, `src/components/audience-room.tsx`, `src/lib/store.ts`, `src/lib/store-persistence.ts`, and `src/lib/domain.ts`; production `.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`; server-local and public `/live/demo-live`, `/host`, and stats returned 200; public `/live/demo-live` contained the Tencent player marker and CDN play URL and did not contain the old audience-facing comment status labels.
- Production hotfix completed on 2026-06-18 for partial iPhone no-picture with Tencent Cloud playback. Root cause evidence: older iPhones/iOS WeChat were less reliable with direct `webrtc://` playback, and Tencent's TCPlayer documentation requires a `<video>` player container while the first integration used a `div`. `TencentCloudLivePlayer` now uses the current TCPlayer CDN script path, renders the required `<video>` container with inline playback attributes, and automatically converts `webrtc://play.fuguilong.cn/live/IHQDAT` to `https://play.fuguilong.cn/live/IHQDAT.m3u8` for iPhone/iPad/iPod user agents while keeping WebRTC playback for Android and non-Apple clients.
- Latest production verification on 2026-06-18 for the iPhone playback hotfix: targeted red/green test `src/components/livekit-audience-player.test.tsx` passed with 4 tests; full `npm run test` passed with 17 files / 80 tests; `npm run lint` passed; `npm run build` passed; local standalone smoke on `http://127.0.0.1:4029/live/demo-live` returned 200 with Tencent player markers and no old audience status labels; `deploy-package.tar.gz` included `.next/standalone/node_modules/next/package.json`, `src/components/tencent-cloud-live-player.tsx`, and `src/components/livekit-audience-player.test.tsx`; production `.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`; server-local and public `/live/demo-live` and `/host` returned 200, contained `tencent-cloud-live-player` and `webrtc://play.fuguilong.cn/live/IHQDAT`, and did not contain `已展示` / `已发送` / `未展示`.

- Production redeploy completed on 2026-06-18 for WeChat OAuth env propagation and the second iPhone no-picture fix. `docker-compose.yml` now explicitly passes `WECHAT_OAUTH_APP_ID` and `WECHAT_OAUTH_APP_SECRET` into the app service; production `.env` was set to `WECHAT_OAUTH_APP_ID=wxb47d8ae28bc0e137`, `WECHAT_OAUTH_APP_SECRET` masked in logs, and `RUNTIME_BASE=current-app:latest`; the app container was force-recreated so Next sees the OAuth env. For iPhone playback, `LiveKitAudiencePlayer` now sends Apple mobile user agents to the LiveKit host-video path instead of Tencent CDN/HLS, while Android/non-Apple clients still use Tencent CDN playback; this avoids the current `play.fuguilong.cn` HTTPS certificate/HLS reliability issue on older iOS.
- Latest production verification on 2026-06-18 for these two fixes: targeted `src/components/livekit-audience-player.test.tsx` passed with 5 tests; full `npm run test` passed with 17 files / 81 tests; `npm run lint` passed; `npm run build` passed; package contents included `docker-compose.yml`, `.next/standalone/node_modules/next/package.json`, `src/components/livekit-audience-player.tsx`, and `src/components/livekit-audience-player.test.tsx`; server-local `/live/demo-live` and `/host` returned 200; container `env` showed `WECHAT_OAUTH_APP_ID` set and `WECHAT_OAUTH_APP_SECRET` present; `/api/live-sessions/demo-live/wechat-profile?viewerId=viewer-env-check&returnTo=https%3A%2F%2Flive.fuguilong.cn%2Flive%2Fdemo-live` returned an `open.weixin.qq.com/connect/oauth2/authorize` authUrl with `source:"wechat-oauth"`; public `/live/demo-live`, `/host`, and the same profile endpoint returned 200; public audience HTML still did not contain `已展示` / `已发送` / `未展示`.

- Production hotfix completed on 2026-06-18 for mobile viewers not seeing the WeChat nickname authorization panel. Root cause: phones that had previously entered during fallback mode already had `wechat-live-viewer-name` in localStorage, so `AudienceRoom` skipped the auth panel and never called the real WeChat OAuth flow. The client now also stores `wechat-live-viewer-name-source`; only a prior `source:"wechat"` authorization can skip the auth panel. Old fallback nickname caches now require re-authorization, so mobile viewers will see the authorization panel and proceed through OAuth.
- Latest production verification on 2026-06-18 for the authorization-cache hotfix: targeted `src/components/audience-room.test.tsx` passed with 13 tests; full `npm run test` passed with 17 files / 83 tests; `npm run lint` passed; `npm run build` passed; package contents included `.next/standalone/node_modules/next/package.json`, `src/components/audience-room.tsx`, and `src/components/audience-room.test.tsx`; production redeploy kept `RUNTIME_BASE=current-app:latest`; server-local and public `/live/demo-live` returned 200 and contained `wechat-auth-panel`; server-local and public `/api/live-sessions/demo-live/wechat-profile?...returnTo=...` returned an `open.weixin.qq.com/connect/oauth2/authorize` authUrl; public audience HTML still did not contain `已展示` / `已发送` / `未展示`.
- Production data correction on 2026-06-18: verification after the authorization-cache redeploy found `demo-live` missing again from the persisted JSON volume, causing `/live/demo-live` to return 500 and `/wechat-profile` to return `LIVE_NOT_FOUND`. The app data file was backed up to `/app/.data/app-store.json.bak-2026-06-18T04-50-29-135Z-demo-live-restore`, `demo-live` and missing stats were restored without pruning existing users/comments/mic data, and `app` was restarted. Server-local and public `/live/demo-live` and `/wechat-profile` returned 200 afterward.

- Production hotfix completed on 2026-06-18 for Tencent Cloud player raw control/support text overlay. Root cause: the first TCPlayer integration loaded the player JavaScript but not `tcplayer.min.css`, so Tencent player controls/support text such as "腾讯云提供技术支持", "暂停", "静音", and playback speed rendered as unstyled raw text over the video. `TencentCloudLivePlayer` now injects the TCPlayer CSS before initializing the player and includes defensive CSS to hide Tencent/player control overlays that are not part of the custom live-room UI.
- Latest production verification on 2026-06-18 for the TCPlayer CSS hotfix: new `src/components/tencent-cloud-live-player.test.tsx` passed; full `npm run test` passed with 18 files / 84 tests; `npm run lint` passed; `npm run build` passed; package contents included `.next/standalone/node_modules/next/package.json`, `src/components/tencent-cloud-live-player.tsx`, and `src/components/tencent-cloud-live-player.test.tsx`; production `/live/demo-live` returned 200 server-local and public; `/wechat-profile?...returnTo=https%3A%2F%2Flive.fuguilong.cn%2Flive%2Fdemo-live` returned a WeChat OAuth authUrl; public audience HTML still did not contain `已展示` / `已发送` / `未展示`.

- Production deploy completed on 2026-06-18 for WeChat Official Account domain verification. Added `public/MP_verify_CWNVKJBgllLqun5p.txt` with content `CWNVKJBgllLqun5p`, rebuilt, packaged, and redeployed with `RUNTIME_BASE=current-app:latest`. Public verification confirmed `https://live.fuguilong.cn/MP_verify_CWNVKJBgllLqun5p.txt` returns 200 with the exact expected content, and `/live/demo-live` still returns 200.

- Production deploy completed on 2026-06-18 for the second WeChat Official Account verification file. Added `public/MP_verify_w9p8pRqdy8818wlZ.txt` with content `w9p8pRqdy8818wlZ` while keeping the older `MP_verify_CWNVKJBgllLqun5p.txt`; rebuilt, packaged, and redeployed with `RUNTIME_BASE=current-app:latest`. Public verification confirmed both `https://live.fuguilong.cn/MP_verify_CWNVKJBgllLqun5p.txt` and `https://live.fuguilong.cn/MP_verify_w9p8pRqdy8818wlZ.txt` return 200 with the expected content, and `/live/demo-live` still returns 200.

- Production config change completed on 2026-06-18 for subscription-account testing mode. Because the current WeChat account is a subscription account without `网页授权获取用户基本信息`, production `.env` was backed up to `.env.bak-disable-wechat-oauth-20260618`, `WECHAT_OAUTH_APP_ID` and `WECHAT_OAUTH_APP_SECRET` were intentionally set empty, and `docker compose up -d --force-recreate app` was run. `/api/live-sessions/demo-live/wechat-profile?...returnTo=...` now returns an automatic fallback nickname with `source:"fallback"` instead of a WeChat OAuth `authUrl`, so viewers can enter the room for testing without OAuth.

## Active Development

- Implement the Prisma-backed repository behind the store repository boundary.
- Keep the new follow-up state model aligned between local JSON and Prisma.
- Keep the local JSON store as the fast preview mode until database setup is ready.

## Deployment Pitfalls Log

- Server port `3000` may already be occupied by another service. Keep this app bound to host port `3001` through Docker Compose and expose it publicly through the existing Caddy reverse proxy.
- Do not overwrite the existing `fuguilong.cn` website. Production live streaming entry is `live.fuguilong.cn`; Caddy routes this subdomain to the live app and routes `/rtc` to LiveKit.
- `live.fuguilong.cn` must have its own DNS `A` record pointing to `218.78.135.21`. Having only the root domain `fuguilong.cn` resolved is not enough; Caddy/Let's Encrypt will fail with `no valid A records found for live.fuguilong.cn`.
- After adding DNS, authoritative DNS can show the new record before recursive resolvers refresh. Verify both `nslookup live.fuguilong.cn a.ezdnscenter.com` and normal `nslookup live.fuguilong.cn`; use `curl --resolve live.fuguilong.cn:443:218.78.135.21` when checking Caddy before caches catch up.
- Mobile camera and microphone capture will not work from plain HTTP on real phones. Use HTTPS (`https://live.fuguilong.cn`) for host broadcasting and audience playback.
- A very large LiveKit UDP range such as `50000-60000/udp` can make Docker spend a long time creating thousands of port mappings on the cloud host and appear stuck during `docker compose up`. The MVP range is limited to `50000-50100/udp`; expand only when capacity testing shows it is needed.
- `50000-50100/udp` does not mean only 100 viewers can watch. It is a UDP media port allocation range. Actual concurrency depends on CPU, bandwidth, LiveKit routing behavior, video bitrate, and client networks.
- The production Docker image depends on local `.next/standalone`. Do not exclude `.next/standalone/node_modules` from the Docker build context or tar package. Root `node_modules` can be excluded, but `.dockerignore` must use `/node_modules` instead of `node_modules`.
- When packaging for deployment, prefer an explicit tar include list containing `.next/standalone` and `.next/static`. Avoid broad `--exclude=node_modules`, because it can also remove `.next/standalone/node_modules` and cause runtime errors such as `Cannot find module 'next'`.
- When redeploying a tar package into `/opt/wechat-live/current`, do not only overlay-extract the new package. Old `.next/static` chunks can remain on disk and still contain removed client code such as audience status badges. Before extracting the package, clean only the generated build artifact directories: `cd /opt/wechat-live/current && rm -rf .next/standalone .next/static`, then extract the tar and rebuild the Docker image.
- When verifying removal of audience-facing comment status labels, avoid a broad deployed-bundle grep alone. The host console can legitimately contain text such as `暂无已展示留言。`; verify the audience live HTML or audience component chunk specifically for absence of `已展示`, `已发送`, and `未展示` near viewer comments.
- Do not run `next build` on the small cloud host during deploy. Build locally first, upload the standalone output, then let Docker build a lightweight runtime image.
- Docker registry mirrors can return `429 Too Many Requests` while resolving `node:22-alpine`. If the server already has `current-app:latest`, set `RUNTIME_BASE=current-app:latest` in `/opt/wechat-live/current/.env` before `docker compose build app` to rebuild from the local runtime image and avoid remote base-image metadata requests.
- For routine Docker disk cleanup, prefer `docker container prune -f`, `docker image prune -f`, `docker network prune -f`, and `docker builder prune -af`. Do not run `docker volume prune` or `docker system prune --volumes` unless there is a verified backup and a deliberate decision to remove database/app data volumes.
- Next 16 may auto-add `.next/dev/types/**/*.ts` to `tsconfig.json`. If `npm run build` fails inside `.next/dev/types/validator.ts` with a generated-type error such as `Cannot find name 'IsExpected'`, treat `.next/dev` as stale dev-server output and regenerate/clean the generated artifact before changing application source.
- The project no longer starts its own Caddy container in production. The existing `weblj-caddy-1` container must be connected to the `current_default` Docker network so it can reach `app:3000` and `livekit:7880`.
- A plain `HEAD /rtc` request can return `404`; that does not prove LiveKit is down. Verify the LiveKit proxy with a WebSocket upgrade request to `/rtc?access_token=bad`; the expected proxy-level signal is LiveKit returning `401 invalid authorization token` and logging the `/rtc` request.
- Be careful when sending remote shell commands from local PowerShell: expressions such as `$(date ...)` can be expanded locally before SSH execution. Avoid `$()` in remote command strings or escape it explicitly.
- Local defaults (`devkey` / `secret`) are development-only. The server `.env` should use a generated `LIVEKIT_API_SECRET` with at least 32 characters; otherwise LiveKit logs `secret is too short`.
- Local `git status` / `git diff` can fail with Git "dubious ownership" because the workspace may be owned by another Windows user while Codex runs as a sandbox user. Do not change global git config just for this; rely on targeted file inspection unless the user explicitly asks to fix git safe.directory.
- In this Windows workspace, `npm` may disappear from PATH in later shell calls even after earlier `npm run test` succeeds. Use the bundled project Node path as a fallback: prepend `D:\AI\LiveKit\.tools\node-v22.21.1-win-x64` to PATH and run `.tools\node-v22.21.1-win-x64\node.exe .tools\node-v22.21.1-win-x64\node_modules\npm\bin\npm-cli.js <script>`. If `Start-Process` fails with duplicate `Path`/`PATH` environment keys, a foreground temporary dev server plus a parallel `Invoke-WebRequest` smoke check is a workable local verification fallback.
- The Browser plugin may be listed as available while its expected `openai-bundled/browser/.../scripts/browser-client.mjs` file is missing from the local cache. Do not bypass the Browser skill by using unrelated browser control; record the limitation and rely on HTTP/build smoke checks unless the plugin cache is repaired.
- The admin can now delete non-live rooms, so `demo-live` may disappear from the local JSON data volume if someone deletes it from the room list. The deployment smoke checks still use `/live/demo-live`; if that route returns `LIVE_NOT_FOUND`, inspect the container volume at `/app/.data/app-store.json`, back it up first, and restore only the missing `demo-live` room record rather than pruning user/comment/mic history. The 2026-06-17 repair backup is `/app/.data/app-store.json.bak-20260617-demo-live-restore`.
- Automatic WeChat nickname authorization requires official-account OAuth configuration to get a real nickname from WeChat: set `WECHAT_OAUTH_APP_ID` and `WECHAT_OAUTH_APP_SECRET` in production and make sure the OAuth callback domain is allowed in the WeChat Official Account backend. Without those env vars, `/api/live-sessions/[id]/wechat-profile` deliberately returns an automatic fallback nickname so viewers are not forced to type.
- If a viewer clicks `同意授权并进入` and the modal appears stuck, check `/api/live-sessions/[id]/wechat-profile` and `/api/live-sessions/[id]/join`. A missing room returns `LIVE_NOT_FOUND`; a stale banned participant returns `USER_BANNED`. As of 2026-06-18, join failures are shown in the modal, but the data root cause still needs correcting in `/app/.data/app-store.json`.
- If the host sees `设备开启失败：USER_BANNED`, verify `/host` is not bound to an old ended room whose `host-1` participant was kicked. The host page now selects the best active/scheduled/draft room via `selectHostConsoleLiveSession`, but production data should still keep the intended room present. The 2026-06-18 repair backup is `/app/.data/app-store.json.bak-20260618-demo-live-restore`.

- Tencent Cloud Live `webrtc://` play URLs are not native HTML video URLs. Do not set `<video src="webrtc://...">`; use the Tencent Cloud web player SDK wrapper (`TencentCloudLivePlayer`) or provide a normal `.flv` / `.m3u8` fallback URL. Keep ordinary audience playback on Tencent Cloud CDN/Live and keep LiveKit for mic users so the 5M upstream server is not used for every viewer's main video.
- For iPhone/iOS WeChat Tencent Cloud playback, prefer HLS (`https://play.fuguilong.cn/live/IHQDAT.m3u8`) over `webrtc://` when compatibility matters more than ultra-low latency. Keep Android on `webrtc://` for lower latency. TCPlayer's container must be a `<video>` element with `playsinline` and `webkit-playsinline`; using a `div` can work in some browsers but is not aligned with Tencent's documented integration and may fail on stricter iOS WebViews.
- When normalizing persisted local JSON data, do not replace `store.liveSessions` with a freshly mapped array on every `getStore()` call. Tests and API handlers may hold the same in-memory store reference; normalize missing fields in place to avoid update/delete operations appearing to succeed while callers inspect stale arrays.

- As of the 2026-06-18 second iPhone fix, Apple mobile viewers are temporarily routed to the LiveKit host-video path instead of Tencent CDN because `https://play.fuguilong.cn/live/IHQDAT.m3u8` produced a certificate principal/trust failure during diagnostics. Fix the Tencent Cloud HTTPS certificate for `play.fuguilong.cn` before switching iPhone viewers back to HLS/CDN; otherwise older iOS/WeChat clients can show a blank video.
- Adding values to `/opt/wechat-live/current/.env` is not enough unless `docker-compose.yml` passes them through under `app.environment`. After adding new runtime env names, redeploy with `docker compose up -d --force-recreate app` and verify inside the container with `docker compose exec -T app env`.

- If a mobile viewer previously entered while OAuth env was absent, their browser may hold `wechat-live-viewer-name` from fallback mode. Do not treat that key alone as real WeChat authorization. The client now requires `wechat-live-viewer-name-source=wechat` before skipping the auth panel; clear both keys manually when debugging a specific phone.

- WeChat OAuth error `10003 redirect_uri域名与后台配置不一致` is not caused by the app's AppID/AppSecret. The app currently generates `redirect_uri=https://live.fuguilong.cn/live/demo-live`, so the WeChat Official Account backend must configure the web authorization domain exactly as `live.fuguilong.cn` (domain only, no `https://`, no path). If this is not configured or has not propagated in WeChat, mobile auth will fail before returning to the app.
- TCPlayer requires both the JavaScript SDK and `tcplayer.min.css`. If the CSS is missing or blocked, player controls can appear as raw Chinese text over the live page. Keep the CSS loader in `TencentCloudLivePlayer`; do not remove it when changing SDK versions.

- WeChat Official Account web authorization domain verification file is now served from the site root: `https://live.fuguilong.cn/MP_verify_CWNVKJBgllLqun5p.txt`.

- Both WeChat Official Account verification files are now served from the site root: `https://live.fuguilong.cn/MP_verify_CWNVKJBgllLqun5p.txt` and `https://live.fuguilong.cn/MP_verify_w9p8pRqdy8818wlZ.txt`.

- Until a certified service account is available, keep `WECHAT_OAUTH_APP_ID` and `WECHAT_OAUTH_APP_SECRET` empty in production to use automatic fallback nicknames for testing. When switching back to real WeChat nicknames, restore service-account credentials, make sure the account has `网页授权获取用户基本信息`, configure `live.fuguilong.cn` as the web authorization domain, then force-recreate the app container.

## Current Session Handoff

- Production deployment is complete for the host layout simplification, mic/manual approval fixes, admin pending-comment polling/top sorting, iPhone playback hardening, and CODE-AUDIT-REPORT.md remediation.
- Verified locally on 2026-06-16 before deploy: `npm run test` passed with 16 files / 57 tests, `npm run lint` passed, `npm run build` passed, `/host` and `/api/live-sessions/demo-live/stats` returned 200 on local dev server `http://127.0.0.1:4018`, heartbeat POST returned 200, and browser smoke checks confirmed host video/mic/comment markers plus no audience-facing comment status labels on `/live/demo-live`.
- Verified in production on 2026-06-16 after deploy: `/opt/wechat-live/current/.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`; server-local `/host`, `/admin`, and `/live/demo-live` returned 200; public `https://live.fuguilong.cn/host`, `https://live.fuguilong.cn/admin`, and `https://live.fuguilong.cn/live/demo-live` returned 200; public `/live/demo-live` did not contain `已展示` / `已发送` / `未展示`; public host HTML contained first-screen mic/comment markers; public stats API returned `ok: true`; app logs showed Next ready.
- Production deployment is complete for the interaction list layout, WeChat nickname display, one-line host status, and real-time online-count fixes.
- Verified in production on 2026-06-16 after deploy: `/opt/wechat-live/current/.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`; server-local `/host`, `/live/demo-live`, and stats API returned 200; public `https://live.fuguilong.cn/host`, `https://live.fuguilong.cn/live/demo-live`, and public stats API returned 200; public host HTML contained `host-status-line` and `host-first-screen-comments`; public audience HTML contained `audience-comments-panel` and no `已展示` / `已发送` / `未展示` status labels.
- Production disk maintenance is complete as of 2026-06-17: safe Docker cleanup reclaimed space from stopped containers, dangling images, unused networks, and BuildKit cache only; volumes were not pruned. Root disk usage went from 49% to 36%, and local/public app health checks still returned 200.
- Production deployment is complete for the audience interaction and multi-mic fixes: audience bottom action now focuses the comment input, the comment composer is inline and borderless, audience mic state can move through apply/pending/connected/reset, audience and host can end a connected mic session, multiple mic video tiles are supported, and the audience title subtitle is hidden.
- Production deployment is complete for the audience overlay/like/nickname authorization fixes, PC admin nickname and real-time online-user fixes, and PC admin live-room edit/delete fixes. Audience live rooms now show title/status/online in the requested corners, use transparent video-overlay comments, support approved join/like interaction rows, and ask first-entry viewers for a WeChat nickname authorization; PC admin tabs now display WeChat nicknames, online users match `currentOnline`, rooms can be edited inline, non-live rooms can be deleted, and live rooms are protected from deletion.
- Verified locally on 2026-06-17 before deploy: `npm run test` passed with 16 files / 74 tests, `npm run lint` passed, `npm run build` passed, and the deployment package included `.next/standalone/node_modules/next/package.json` plus the changed audience/admin/API files.
- Verified in production on 2026-06-17 after deploy: `/opt/wechat-live/current/.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`; server-local `/admin`, `/host`, `/live/demo-live`, stats, participants, mic requests, comment analytics, and leads endpoints returned 200; public `https://live.fuguilong.cn/admin`, `/host`, `/live/demo-live`, stats, participants, mic requests, comment analytics, and leads endpoints returned 200; public audience HTML contained `audience-comments-overlay`, `audience-title-overlay`, `audience-status-overlay`, and `wechat-auth-panel`, did not contain `LiveKit Room`, and did not contain `已展示`, `已发送`, or `未展示`; public admin HTML contained edit/delete controls and the online-user tab; app logs showed Next ready.
- Final production correction on 2026-06-17: after the release package was refreshed with the latest `AGENTS.md`, `/live/demo-live` briefly returned 500 because the persisted JSON volume was missing the `demo-live` room. The data file was backed up to `/app/.data/app-store.json.bak-20260617-demo-live-restore`, `demo-live` was restored in `liveSessions`, `app` was restarted, and server-local plus public `/live/demo-live`, `/api/live-sessions/demo-live`, `/stats`, and `/participants` returned 200 again.
- Production deployment is complete for automatic audience nickname authorization as of 2026-06-18. The viewer no longer types a nickname; the modal button fetches `/api/live-sessions/[id]/wechat-profile`, joins with the returned nickname, and records the approved `某某进入直播间了` row. Current production fallback returns automatic names when WeChat OAuth env vars are missing; configure `WECHAT_OAUTH_APP_ID` / `WECHAT_OAUTH_APP_SECRET` plus the WeChat OAuth domain to obtain real WeChat nicknames.
- Verified in production on 2026-06-18 after deploy: `/opt/wechat-live/current/.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`; server-local `/live/demo-live` and `/api/live-sessions/demo-live/wechat-profile?viewerId=viewer-auto` returned 200 with auth panel present, `自动获取昵称` present, and `输入微信昵称` absent; public `https://live.fuguilong.cn/live/demo-live`, `/admin`, `/api/live-sessions/demo-live/wechat-profile?viewerId=viewer-auto`, and stats returned 200; public audience HTML did not contain `已展示`, `已发送`, or `未展示`.
- Production hotfix is complete as of 2026-06-18 for the host device `USER_BANNED` error and audience authorization no-response symptom. Host console room selection now avoids old ended rooms when a scheduled/live/draft room is available, audience authorization displays join failures, and `demo-live` was restored in the production JSON volume after backing up `/app/.data/app-store.json` to `/app/.data/app-store.json.bak-20260618-demo-live-restore`.
- Verified in production on 2026-06-18 after hotfix deploy: server-local `/host`, `/live/demo-live`, and `/api/live-sessions/demo-live/wechat-profile?viewerId=viewer-debug` returned 200; public `/host`, `/live/demo-live`, `/api/live-sessions/demo-live/wechat-profile?viewerId=viewer-debug`, and host token POST for `host-1` returned 200; the host token contained `private-demo-live-host-1` and did not contain `USER_BANNED`; public audience HTML still had `wechat-auth-panel`, did not contain `输入微信昵称`, and did not contain `已展示`, `已发送`, or `未展示`.
- Production deployment is complete as of 2026-06-18 for Tencent Cloud Live audience playback. Ordinary audience main video now renders through the Tencent Cloud player using `webrtc://play.fuguilong.cn/live/IHQDAT`, while LiveKit remains connected for audience mic publishing and remote mic video tiles. The host page still uses the existing LiveKit mobile host flow; this first CDN step assumes the Tencent Cloud stream `IHQDAT` is being pushed separately to `webrtc://push.fuguilong.cn/live/IHQDAT?...`.
- Verified in production on 2026-06-18 after Tencent Cloud playback deploy: local standalone, server-local, and public `/live/demo-live` returned 200 with `tencent-cloud-live-player` and the CDN play URL present; server-local and public `/host` and stats returned 200; public audience HTML still did not contain `已展示`, `已发送`, or `未展示`; `/opt/wechat-live/current/.env` kept `RUNTIME_BASE=current-app:latest`; Docker Compose rebuilt `current-app:latest` and restarted `app`.
- Production iPhone playback hotfix is complete as of 2026-06-18. Older iPhone/iOS WeChat clients now receive HLS fallback playback derived from the Tencent WebRTC URL (`https://play.fuguilong.cn/live/IHQDAT.m3u8`), the Tencent player container is now the documented `<video>` element, and Android/non-Apple viewers still use `webrtc://play.fuguilong.cn/live/IHQDAT`.
- Verified in production on 2026-06-18 after iPhone hotfix deploy: `npm run test`, `npm run lint`, and `npm run build` all passed locally before deploy; production server-local and public `/live/demo-live` plus `/host` returned 200; public audience HTML contains the Tencent player marker and CDN URL and still does not contain `已展示`, `已发送`, or `未展示`.
- Production deployment is complete as of 2026-06-18 for real WeChat OAuth env propagation and the follow-up iPhone playback path change. The app container now has `WECHAT_OAUTH_APP_ID` and `WECHAT_OAUTH_APP_SECRET`, and `/wechat-profile` returns a WeChat OAuth `authUrl` instead of fallback nickname when `returnTo` is supplied. Apple mobile viewers now use LiveKit host-video playback because the Tencent HLS fallback URL currently has an HTTPS certificate/trust problem; Android/non-Apple viewers continue using Tencent CDN playback.
- Verified in production on 2026-06-18 after redeploy: server-local and public `/live/demo-live` and `/host` returned 200; server-local and public `/api/live-sessions/demo-live/wechat-profile?...returnTo=...` returned 200 with `open.weixin.qq.com/connect/oauth2/authorize`; container env confirmed the app ID and secret are present; public audience HTML still did not contain `已展示`, `已发送`, or `未展示`.
- Production deployment is complete as of 2026-06-18 for the mobile WeChat authorization-cache fix. Phones with old fallback nickname cache should now show the authorization panel instead of silently entering; only phones that have already completed real OAuth (`wechat-live-viewer-name-source=wechat`) skip it. `demo-live` was restored again in the production JSON volume after backing up `/app/.data/app-store.json` to `/app/.data/app-store.json.bak-2026-06-18T04-50-29-135Z-demo-live-restore`.
- Verified in production on 2026-06-18 after this hotfix: server-local and public `/live/demo-live` returned 200 with `wechat-auth-panel`; server-local and public `/api/live-sessions/demo-live/wechat-profile?...returnTo=...` returned 200 with a WeChat OAuth `authUrl`; public audience HTML did not contain `已展示`, `已发送`, or `未展示`.
- Production deployment is complete as of 2026-06-18 for the TCPlayer CSS/control-text hotfix. The Tencent player CSS is loaded by the client component and defensive CSS hides Tencent/player control overlays, so the custom live-room UI should no longer show raw "腾讯云提供技术支持/暂停/静音/播放速度" text over the video.
- Verified in production on 2026-06-18 after this hotfix: server-local and public `/live/demo-live` returned 200; server-local and public `/api/live-sessions/demo-live/wechat-profile?...returnTo=...` returned 200 with a WeChat OAuth authUrl whose `redirect_uri` is `https://live.fuguilong.cn/live/demo-live`; public audience HTML did not contain `已展示`, `已发送`, or `未展示`.
- WeChat domain verification file deployment is complete as of 2026-06-18. The file `MP_verify_CWNVKJBgllLqun5p.txt` is in `public/`, deployed to production, and public URL verification returned 200 with `CWNVKJBgllLqun5p`.
- WeChat domain verification file deployment is complete for both verification tokens as of 2026-06-18. Public URL verification returned 200 for `MP_verify_CWNVKJBgllLqun5p.txt` with `CWNVKJBgllLqun5p` and for `MP_verify_w9p8pRqdy8818wlZ.txt` with `w9p8pRqdy8818wlZ`.
- Production is currently in subscription-account test mode as of 2026-06-18: WeChat OAuth env vars are blank, `/wechat-profile` returns fallback automatic nicknames, and viewers can enter without real WeChat nickname authorization. Public verification confirmed `/wechat-profile?...returnTo=...` returns `source:"fallback"` and no `open.weixin.qq.com` auth URL, while `/live/demo-live` returns 200 with the auth panel.
- Production hotfix deployed on 2026-06-22 for audience video stuck/loading after the Tencent Cloud player integration: the root cause was that ordinary audience playback defaulted to `cdnPlayUrl`/Tencent Cloud while the mobile `/host` console still publishes only to LiveKit, so viewers could wait on a CDN stream that was not being pushed by the app. `LiveKitAudiencePlayer` now defaults audience main playback to the LiveKit host track even when `cdnPlayUrl` exists, and Tencent Cloud playback is used only when `NEXT_PUBLIC_AUDIENCE_CDN_ENABLED=true` is explicitly baked into the build. Keep that flag disabled until Tencent Cloud push/LiveKit egress is actually wired and the `play.fuguilong.cn` HTTPS certificate issue is fixed.
- Latest verification on 2026-06-22 for the audience video hotfix: the new regression test first failed because the Tencent player was called by default, then passed after the flag-gated fix; full `npm run test` passed with 18 files / 85 tests, `npm run lint` passed, and `npm run build` passed. `deploy-package.tar.gz` was rebuilt with a whitelist package so `.next/standalone/node_modules/next/package.json` was included, uploaded to `/opt/wechat-live/releases/deploy-package.tar.gz`, extracted into `/opt/wechat-live/current` after cleaning remote `.next/standalone` and `.next/static`, and redeployed with Docker Compose while keeping `RUNTIME_BASE=current-app:latest`. Public `https://live.fuguilong.cn/live/demo-live`, `/host`, and stats returned 200; app logs showed Next ready; the deployed client bundle contains the `NEXT_PUBLIC_AUDIENCE_CDN_ENABLED` gate and production HTML does not expose that flag as enabled.
- Next recommended product step: test viewer entry with this fallback mode. Later, after obtaining a certified service account, replace the production `.env` with the new service-account AppID/AppSecret and re-enable real OAuth.
- Reusable server deployment reference added on 2026-07-14: `服务器 Docker、PostgreSQL、Caddy 多软件部署指南.md` documents this host's Docker, PostgreSQL, Caddy, ports, LiveKit route, deployment, rollback, and cleanup constraints for safely adding another software service without impacting existing sites.
- Authentication & authorization system deployed on 2026-07-22:
  - Real password login replaces hardcoded client-sent `actorId` in all management API routes.
  - `src/lib/password.ts`: scrypt password hashing and verification (zero deps, uses `node:crypto`).
  - `src/auth.ts`: NextAuth Credentials provider verifies password against `passwordHash`.
  - `src/lib/auth-helpers.ts`: `requireAuth(roles?)` extracts authenticated userId from server-side JWT session.
  - `src/proxy.ts`: protects `/admin`, `/host` (redirect to login) and management API patterns (401). Authoritative role checks remain in pages and route handlers.
  - Demo user passwords seeded: `admin-1/admin123` (super_admin), `host-1/host123` (host), `moderator-1/mod123` (moderator), `director-1/director123` (director).
  - `normalizeStore` auto-assigns default password hashes to existing JSON store users without `passwordHash`.
  - 14 management API routes (start, end, DELETE room, comment approve/reject/pin/mark-question/delete, mic approve/reject/end, participant kick/mute/unmute) now use `requireAuth()`.
  - `admin-console.tsx` and `mobile-host-console.tsx` no longer send hardcoded `actorId`.
  - `SessionProvider` added to root layout.
  - Verification: 82/82 tests pass, TypeScript + Next.js build succeed.
  - WeChat domain verification file `MP_verify_i8H2f7tIAd0LGbmT.txt` added to `public/`.
  - `.env.example` updated with `replace-with-*` security placeholders and `WECHAT_OAUTH_*` variables.
  - Build package `deploy-package.tar.gz` ready at project root (scp timed out from sandbox; needs manual upload + Docker Compose rebuild).
- Code audit remediation and production hardening on 2026-07-22:
  - `src/lib/http.ts`: `readJson` now throws `INVALID_JSON` instead of silently returning `{}` on parse failure.
  - `src/components/audience-room.tsx`: fallback-nickname users no longer see the auth panel on return visits (skips auth when `storedNameSource === "fallback"`).
  - `src/app/api/live-sessions/[id]/comments/route.ts`: comment content limited to 500 characters via Zod `.max(500)`.
  - `src/lib/live-service.ts`: `endLiveSession` replay URL changed from non-existent `/replays/{id}.mp4` to actual API path `/api/live-sessions/{id}/replay`.
  - Verification: 82/82 tests pass, TypeScript + Next.js build succeed with zero errors.
- Additional production hardening on 2026-07-22:
  - `src/app/admin/error.tsx` and `src/app/host/error.tsx`: Error Boundaries added for admin panel and host console, with user-friendly error messages for `LIVE_NOT_FOUND` and `AUTH_REQUIRED`.
  - `docker-compose.yml`: app container healthcheck added (HTTP GET /api/live-sessions every 30s).
  - Verification: 82/82 tests pass, TypeScript + Next.js build succeed.
- Live room access password feature on 2026-07-22:
  - `domain.ts`: `LiveSession` model adds optional `accessPassword` field.
  - `POST /api/live-sessions`: `createLiveSchema` accepts `accessPassword`.
  - `PATCH /api/live-sessions/[id]`: `livePatchSchema` accepts `accessPassword`.
  - `POST /api/live-sessions/[id]/join`: validates `accessPassword` against the live session before allowing join; returns `ACCESS_PASSWORD_INCORRECT` on mismatch. Rooms without a password skip the check.
  - `GET /api/live-sessions` and `GET /api/live-sessions/[id]`: `accessPassword` stripped from API responses to avoid leaking the password to clients.
  - `live/[id]/page.tsx`: passes `hasAccessPassword={Boolean(live.accessPassword)}` to AudienceRoom, strips `accessPassword` from the serialized props.
  - `AudienceRoom`: accepts `hasAccessPassword` prop, adds `passwordVerified`/`passwordError`/`isVerifyingPassword` state.
  - **Manual step needed**: open `src/components/audience-room.tsx` and add a `submitAccessPassword()` function (calls `/join` with `{ accessPassword }`) and a password prompt overlay that shows when `!passwordVerified && hasAccessPassword`. The overlay should have a password input, error display, and confirm button. After successful verification, set `passwordVerified(true)`.
  - AudienceRoom UI fully implemented after user added the function and overlay manually. Build and tests pass. 7 files modified for this feature.
- PostgreSQL persistence layer added on 2026-07-22:
  - `prisma/schema.prisma`: added `passwordHash` to User, `cdnPlayUrl`/`accessPassword` to LiveSession for domain-model parity.
  - `src/lib/store-repository-prisma.ts`: `PrismaStoreRepository` implements `StoreRepository` — reads all data from PostgreSQL via Prisma, writes back in transactions, falls back to JSON when PG is unavailable. File-level `@ts-nocheck` present until Prisma client is regenerated.
  - `src/lib/store-repository.ts`: `usePgStorage()` helper checks `DATABASE_STORAGE` env var.
  - `src/lib/store.ts`: `initPrismaStore()` called on first request when `DATABASE_STORAGE=enabled` — loads from PG and swaps the in-memory cache, then persists all future saves to both JSON and PG.
  - To enable: add `DATABASE_STORAGE=enabled` to `.env`, ensure PostgreSQL is running, run `npx prisma generate && npx prisma migrate deploy`, then restart the app. The first request will seed PG from existing JSON data.

## Next Development Queue

1. Replace the single-process PostgreSQL snapshot repository with request-scoped incremental Prisma queries before horizontal scaling.
   - Production mode now uses PostgreSQL only when `DATABASE_STORAGE=enabled`; JSON is local-development storage only.
2. Add WeChat JS-SDK/share landing integration.
3. Add export and search for comments, share ranking, and customer leads.
4. Rotate LiveKit API key/secret for production and add TURN/TLS if WeChat/corporate networks need stronger connectivity.

## 2026-07-23 Authentication, Password Room, and PostgreSQL Closure

- Management room creation and PATCH now require authenticated `super_admin`, `director`, or `moderator`; authorization uses the current database user role rather than trusting the role embedded in an old session.
- Staff LiveKit token requests must match the authenticated user ID and role, preventing callers from selecting `host` or `moderator` in the request body.
- Room passwords are encrypted with AES-256-GCM using `ROOM_PASSWORD_ENCRYPTION_KEY`; public DTOs expose only `hasAccessPassword`. The authenticated management endpoint decrypts the password for authorized staff.
- Password verification issues a two-hour HttpOnly room-access cookie bound to live ID, viewer ID, and password version. Join, heartbeat, comments, audience events, mic application/end, and audience token issuance enforce it.
- Password edits preserve the current password when unchanged, support explicit clearing, increment the password version, and invalidate earlier room cookies.
- `prisma/migrations/20260723000000_auth_room_password_closure/migration.sql` adds authentication, encrypted-password, CDN, nickname, and activity fields needed by the runtime domain.
- `src/instrumentation.ts` initializes `PrismaStoreRepository` before serving requests when `DATABASE_STORAGE=enabled`. PostgreSQL connection failure is fatal and no JSON fallback occurs in this mode. JSON remains the local-development repository.
- The current PostgreSQL repository keeps one in-process domain snapshot and persists it in a transaction. This is acceptable only for the current single-instance MVP; request-scoped incremental Prisma operations are required before horizontal scaling.
- Next.js 16 routing protection moved from deprecated `src/middleware.ts` to `src/proxy.ts`; route/page authorization remains authoritative.
- Operational documentation was rewritten in `README.md`, including required environment variables, migration order, password-key handling, validation, smoke tests, rollback, and the remaining single-instance constraint.

## Verification Rule

Before reporting development complete, run:

- `npm run test`
- `npm run lint`
- `npm run build`

Also smoke-test the touched API/page when a local dev server is available.

## Deployment Pitfalls (踩过的坑)

### 7z tar 路径问题
7-Zip 使用绝对路径打包时，tar 内的文件路径会包含 `D:\AI\LiveKit\...` 前缀。
在 Linux 服务器上 `tar -xzf` 解压后文件全部错位，Docker build 找不到 `.next/standalone/`。
**修复**：用 Git Bash 的 `tar` 从项目根目录以相对路径打包：
```bash
cd /d/AI/LiveKit
tar -cf deploy-package.tar Dockerfile docker-compose.yml AGENTS.md README.md .next/standalone .next/static src/components src/lib src/app src/types src/proxy.ts src/instrumentation.ts src/auth.ts prisma
```
然后用 7z 压缩为 gzip：`7z a -tgzip deploy-package.tar.gz deploy-package.tar`

### Git Bash scp 路径格式
Git Bash 不支持 Windows 反斜杠路径。`scp D:\AI\LiveKit\...` 会报 `No such file or directory`。
**修复**：在 Git Bash 中使用正斜杠相对路径：`scp deploy-package.tar.gz root@host:/path/`

### apply_patch 编辑回滚
`apply_patch` 添加新行时，如果上下文匹配已有行，不会自动替换。例如 `createLive` 函数中的 `body` 行，
新行被插入为重复行导致 `An object literal cannot have multiple properties with the same name`。
**修复**：手动检查并删除旧行。

### 沙箱文件写入限制
`C:\Program Files\7-Zip\7z.exe` 和 `C:\Program Files\Git\usr\bin\tar.exe` 打包正常，
但 `scp`/`ssh` 无法从沙箱发起网络连接（Permission denied / timeout），
也无法创建新的 `src/app/` 子目录。部署和新建子目录需要用户手动操作。

### audidence-room.tsx 编译连环错误
1. `submitAccessPassword` 函数意外放在 `return (` 内部 → `Expected ',' or ')'`
2. 密码弹窗 JSX 与 `<main>` 同级缺少 Fragment 包裹 → `Expected } but found EOF`
3. Fragment 关闭处写成 `<>` 而非 `</>`  → `Unexpected token`
4. 删除重复 `export function AudienceRoom` 后保留正确的函数签名

### PrismaStoreRepository TypeScript 类型
Prisma schema 更新后未运行 `prisma generate`，导致 Prisma Client 类型与实际字段不匹配。
`@ts-nocheck` 临时跳过，部署前需要在服务器上 `npx prisma generate && npx prisma migrate deploy`。

### Turbopack 构建字体下载
构建时 Turbopack 尝试从 `fonts.gstatic.com` 下载 Google Fonts，
网络受限时构建报错 `Module not found: Can't resolve '@vercel/turbopack-next/internal/font/google/font'`。
重试后通常能通过；如果持续失败，考虑在 `next.config.ts` 中禁用 Google Fonts 或用本地字体替代。
